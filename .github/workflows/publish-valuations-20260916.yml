name: One-time valuation report publisher 2026-09-16

on:
  push:
    paths:
      - '.github/workflows/publish-valuations-20260916.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect-transfer:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect firestorage share bootstrap
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL -o /tmp/page.html 'https://firestorage.ai/ja/f/McFRreXLULej'
          echo '=== PAGE ==='
          cat /tmp/page.html
